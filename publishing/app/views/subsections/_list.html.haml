%div.col-md-6
  = render partial: 'subsections/new', locals: { subsection: Subsection.new }
  -subsections.each do |subsection|
    = render partial: 'subsections/editor', locals: { subsection: subsection }
    = render partial: 'subsections/new', locals: { subsection: subsection }

%div.col-md-6
  = render partial: 'subsections/preview', locals: { star: star }
