=form_for(@chapter, html: {class: "form-horizontal", role: "form"}) do |f|
  - if @chapter.errors.any?
    %div.error_explanation
      %h2= #{ pluralize(@chapter.errors.count, "error") } prohibited this chapter from being saved:
      %ul
        -@chapter.errors.full_messages.each do |message|
          %li= message

  %div.form-group
    = f.label :title, class: "col-sm-2 control-label"
    %div.col-sm-10
      = f.text_field :title, size: 120

  %div.form-group
    = f.label :description, class: "col-sm-2 control-label"
    %div.col-sm-10
      = f.text_area :description, cols: 120, rows: 20

  - if @chapter.id
    %div.form-group
      %div.col-sm-offset-2.col-sm-10
        = link_to 'cancel', @chapter, class: "btn btn-default"
        = submit_tag('update', class: "btn btn-primary")
  - else
    %div.form-group
      %div.col-sm-offset-2.col-sm-10
        = link_to 'cancel', chapters_path, class: "btn btn-default"
        = submit_tag('save', class: "btn btn-primary")
